gftools==0.9991
fontmake==3.12.1

# scripts/contrast.py, worklist.py, proof.py, datasheet.py
numpy
scipy
matplotlib

# Fontbakery is deliberately absent: its googlefonts extra pins protobuf<4
# while gftools needs protobuf>=6.33, so the two cannot share an environment.
# See requirements-qa.txt, installed separately by the QA job.
